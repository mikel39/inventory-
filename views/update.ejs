<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit a book</title>
</head>

<body>
    <form action="/edit/book/<%= book.id %>" method="POST">
        <div>
            <label for="title">Title</label>
            <input type="text" name="title" id="title" value="<%= book.booktitle %>" required />
        </div>
        <div>
            <label for="date">Release Date</label>
            <input type="date" name="date" id="date" value="<%= book.rdate %>" required />
        </div>
        <div>
            <label for="rating">Rating</label>
            <input type="number" name="rating" id="rating" max="5" min="0" step="0.01"
                value="<%= book.bookrating.toFixed(2) %>" required />
        </div>
        <div>
            <label for="pages">Pages</label>
            <input type="number" name="pages" id="pages" value="<%= book.bookpages %>" required />
        </div>
        <div>
            <label for="genre">Genre</label>
            <input type="text" name="genre" id="genre" value="<%= book.genretag %>" required>

        </div>
        <div>
            <label for="author">Author</label>
            <input type="text" name="author" id="author" value="<%= book.author %>" />
        </div>
        <div>
            <label for="image">Image Url</label>
            <input type="url" name="image" id="image" value="<%= book.bookimage %>" />
        </div>
        <div>
            <label for="description">Description</label>
            <textarea name="description" id="description"><%= book.bookdescription %></textarea>
        </div>
        <button type="submit">Submit</button>
    </form>
</body>

</html>